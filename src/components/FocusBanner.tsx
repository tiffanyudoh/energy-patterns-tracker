/**
 * Focus Banner Component (Spec 3.3 - Placeholder for Phase 3)
 *
 * - Position: Top of daily logging interface
 * - Auto-height: collapses when no focus is set
 * - Will display active weekly focus in Phase 3
 */
export function FocusBanner() {
  // Placeholder for Phase 3 - returns empty div that collapses
  // Will be implemented in Phase 3 with focus selection
  return <div className="empty:hidden" />;
}
